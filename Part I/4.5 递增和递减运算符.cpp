#include<iostream>
#include<vector> 
using namespace std;

int main()
{
	int i=0,j;
	j=++i;
	cout << j << " ";
	j=i++; 
	cout << j;
	
	vector<int> v;
	auto pbeg=v.begin();
	while (pbeg!=v.end() && *beg>=0)
		cout >> *pbeg++ << endl;
	
	//赋值号两端修改同一变量时需要分步 
	for (auto it=s.begin(); it!=s.end() && !isspace(*it); ++it)
		*it=toupper(*it)
	while (beg!=s.end() && !isspace(*beg))
		*beg=toupper(*beg++); //错误。 
	
	return 0;
}

