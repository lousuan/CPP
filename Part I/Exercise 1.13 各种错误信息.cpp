#include <iostream>
using namespace std; 
int main(   // error: missing ')' in parameter 
{ 
    int v1, v2; 
    
    cin >> v >> v2; // error: uses " v "not" v1" 
    cout << v1 + v2 << endl: // error: used colon, not a semicolon 
    cout << Update master. << endl;// error: missing quotes around

    getchar();
    getchar();
    return 0 
}
